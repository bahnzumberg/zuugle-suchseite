const popupContent = {
    textAlign: "center",
    height: "50px",
    marginTop: "15px",
    curser:"pointer"
  };
  const popupHead = {
    fontWeight: "bold",
    fontSize: "15px",
    marginBottom: "10px"
  };
  
  const popupText = {
    fontSize: "15px",
    marginBottom: "20px"
  };
    
  export { popupContent, popupHead, popupText };
  